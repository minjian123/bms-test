# Kiwi 用例台账

> 平台策展用例的批次台账与对账留痕 · 用例本体在 Kiwi TCMS，不在 git

## 1. 口径 <a id="rule"></a>

- **两个产品分开看**：`BMS 基础管理系统` 承载**策展用例**（人工设计、一任务一条，含编号）；`BMS 自动化用例（CI）` 是 CI 导入的执行记录（一函数一条，数量大、编号连片）——**本台账只记策展用例**。
- **编号是平台全局自增且跨产品共享**：策展用例编号必然跳号，登记后以 `scripts/kiwi/register_cases.py` 回读的 `case_id` 为准（实证：策展第 70 条为 528，阶段四首条为 698）。
- **本台账不复制用例正文**（正文在平台，含步骤与预期），只记「批次 / 范围 / 编号 / 状态 / 对账」；需要正文时用 `export_cases.py` 导出或用平台网页查。
- 状态语义：`CONFIRMED` = 现行有效；`DISABLED` = 已停用（保留编号与历史，不复用）。
- 数据来源：`scripts/kiwi/exports/` 下的平台快照 + 各批次登记输入（`scripts/kiwi/cases/`）。

## 2. 批次台账 <a id="batches"></a>

> 依据 2026-09-17 平台快照（`scripts/kiwi/exports/2026-09-17_策展用例快照.json`，共 **150 条**；同日 03-02-01 批次登记后刷新）；2026-09-17 起批次（723 起）以登记回读为准。
> 编号簇为「任务号 + 标题」前缀者可直接定位任务；早期无前缀的（1 ~ 61）属阶段一 / 二基座摸底期登记。
> **2026-09-19 补记**：阶段四域 08 的 760 ~ 764 批次（`08_01` 三嵌套 / `08_02` / `08_03_01`）平台已登记但当时未入本台账，按平台回读补记（见 §2 第 34 ~ 36 行）；同日 `08_03_02` 登记 **765**（见 §2 第 37 行）、`08_03_03` 登记 **766**（见 §2 第 38 行）。
> **2026-09-19 再补记**：`08_04` 权限配置两嵌套子任务登记 **767 / 768**（当时已记代码标注与任务测试记录，未入本台账，按平台回读补记，见 §2 第 39 行）；`08_05` 导入导出两嵌套子任务登记 **769 / 770**（`08_05_01` / `08_05_02` 均已实施完成并落代码标注，见 §2 第 40 行与 §3）；`08-6-1` 表单设计器登记 **771**（已实施完成并落代码标注，见 §2 第 41 行与 §3）；**2026-09-19 四补记**：`08-6-2` 表单渲染器登记 **772**（已实施完成并落代码标注，见 §2 第 42 行与 §3）；**2026-09-19 五补记**：`08_07` 国际化文案编辑器两嵌套子任务登记 **773 / 774**（`08-7-1` / `08-7-2` 均已实施完成并落代码标注，见 §2 第 43 / 44 行与 §3），至此阶段四域 08 的 `08_01` ~ `08_07` 主线用例全部入账；**域 06 / 07 的 6 个任务仍无平台编号与代码标注**（见 §6 待办 5）。
> **2026-09-19 六补记**：`08_08` 审批流展示与流程建模器两嵌套子任务登记 **775 / 776**（`08-8-1` / `08-8-2` 均已实施完成并落代码标注，见 §2 第 45 / 46 行与 §3），至此阶段四域 08 的 `08_01` ~ `08_08` 主线全部入账；`08_09`（报表设计器与大屏设计器与播放）/ `08_10`（AI 助手）随对应后端能力窗口待登记。
> **2026-09-21 补记（编号归属更正 + 本批补登）**：经平台回读，上文 2026-09-19 六补记的 **775 / 776 归属有误**——775 实为 **07-6 图表卡**、776 / 777 实为 **`08-9-1` 报表设计器 / `08-9-2` 大屏设计器与播放**；**`08_08`（审批流展示 / 流程建模器）在平台无独立用例**，其代码 `kiwi_id` 标注（775 / 776）属错标（见 §6 待办 6）。同批补记 778 ~ 781 / 906 / 927（后端补充基座）、958 ~ 966（前端后续批次与后端字典）与 **967 ~ 973**（域 05 与域 06/07 首批补登，见 §3）。

| # | 编号簇 | 条数 | 范围（按用例主题） | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 1 ~ 10 | 10 | 骨架链路：根路由 / 健康检查 / demo CRUD（含 404 分支） | CONFIRMED |
| 2 | 11 ~ 18 | 8 | 后端基类：`BaseRepository` / `BaseService` / `BaseSchema` / `BaseObject` / 集合与 Redis 封装 | CONFIRMED |
| 3 | 19 ~ 26 | 8 | 前端基线（双端各一）：默认页冒烟、基础类（契约类型 / `BaseApi` / store / 稳定序列化）、公共组合式与工具、Axios 基线 | CONFIRMED |
| 4 | 27 ~ 61 | 35 | 阶段一 / 二基座与能力域占位：模块注册表、模型与异常、配置 / 安全 / 日志 / 数据访问底座、租户拓扑，以及 21 个能力域横切基座（脱敏 / 锁 / 验证码 / 降级熔断 / 限流幂等 / 可观测 / 健康 / OAuth / 文件 / LLM / 检索 / 通知 / 实时推送 / 出站 / 工作流 / IdP / 查询提供者 / 导入导出 / 审计哈希链 / 归档 / 字段类型 / i18n / 工作台卡片） | CONFIRMED |
| 5 | 62 ~ 69 | 8 | 阶段一 03 / 04 域：配置管理、日志体系、健康检查（2 条）、CI 流水线激活、三库集成收口、M1 门禁核对、测试报告与 README | CONFIRMED |
| 6 | 528 | 1 | 阶段一 09- 重验证层分档激活与 Kiwi 导入接线 | CONFIRMED |
| 7 | 529 ~ 530 | 2 | 阶段三 01 域：中间层与注册表（529）、解析器与契约版本（530） | CONFIRMED |
| 8 | 531 / 565 / 532 / 566 | 4 | 阶段三 02 域与 01-3：能力域基类改继承（531）、提供者改经 `resolve_plugin`（565）、`null.py` 拆分迁移（532）、插件清单接口（566） | CONFIRMED |
| 9 | 567 ~ 568 | 2 | 阶段三 03 域：契约测试基座（567）、域注册表与实现覆盖（568） | CONFIRMED |
| 10 | 651 / 652 / 655 | 3 | 阶段三 03 域：`BaseProvider` 提取（651）、组合轨边界（652）、域注册表收敛（655） | CONFIRMED |
| 11 | 661 ~ 664 | 4 | 阶段三 04 域：对象存储 local（661）、minio（662）、M3 门禁核对（663）、阶段测试报告归档核对（664） | CONFIRMED |
| 12 | 675 ~ 678 | 4 | 阶段三 05 域：过渡物清理核验（675）、MinIO 真实连通（676）、四端口 Null 与全量接线（677）、health Null 登记口径对齐（678） | CONFIRMED |
| 13 | 688 / 689 | 2 | 阶段一 收口后补登：模块边界护栏批 1（688）、事件基座占位契约（689） | CONFIRMED |
| 14 | 697 | 1 | 阶段四前期误登记（「能力域占位：选项源 / 用户展示 / 富文本净化 / 上传编排」）——组件设计阶段只出设计文档，不落后端代码与用例 | **DISABLED**（保留编号不复用） |
| 15 | 698 / 699 | 2 | **阶段四 02-01 根系基类**（frontend/apps/desktop / frontend/apps/mobile 各一，同接口多实现断言集） | CONFIRMED |
| 16 | 700 ~ 704 | 5 | **阶段四 02-02 组件根基类**（组件根 / 占位 / 异步资源 / 错误 / 订阅；双端共用编号，一端一套断言集） | CONFIRMED |
| 17 | 705 ~ 708 | 4 | **阶段四 02-03 能力机制与预置能力**（能力机制 / 注册表基座 / 核心 12 能力 / 复用 19 能力；双端共用编号） | CONFIRMED |
| 18 | 709 ~ 712 | 4 | **阶段四 02-04 域基类**（输入域 / 展示域 / 树域 / 编辑器域；双端共用编号） | CONFIRMED |
| 19 | 713 ~ 716 | 4 | **阶段四 02-05 扩展与登记机制**（继承护栏 / 依赖边界 / 清单对账 / 演练与错误码；双端共用编号） | CONFIRMED |
| 20 | 717 / 718 | 2 | **阶段四 02-06-01 请求封装**（请求层 / 组合式；双端共用编号） | CONFIRMED |
| 21 | 719 / 720 | 2 | **阶段四 02-06-02 权限指令**（指令 / 判定与 store；双端共用编号） | CONFIRMED |
| 22 | 721 / 722 | 2 | **阶段四 02-06-03 格式化工具**（格式化 / 注册表与校验器；双端共用编号） | CONFIRMED |
| 23 | 723 | 1 | **阶段四 03-06 基础令牌**（五组令牌 / 引用映射 / EP·Vant 映射 / 未定义告警；双端共用编号） | CONFIRMED |
| 24 | 724 / 725 | 2 | **阶段四 03-01 弹窗抽屉表单**（容器三态 / 尺寸 / 拦截链 / 提交与权限；组合式 useFormModal / useConfirm；仅 frontend） | **DISABLED**（apps 侧旧件停用，2026-09-17） |
| 25 | 726 ~ 730 | 5 | **阶段四 03-02 异常与空状态**（错误页 / 空状态 / 骨架屏 / 内容遮罩 / 反馈状态机；仅 frontend） | **DISABLED**（apps 侧旧件停用；移动端补齐另登 751 ~ 755） |
| 26 | 731 ~ 736 | 6 | **阶段四 03-03 结构布局**（多标签导航 / 树形主从 / 基础布局件；仅 frontend） | **DISABLED**（apps 侧旧件停用，2026-09-17） |
| 27 | 737 / 738 | 2 | **阶段四 03-04 侧边菜单**（渲染 / 高亮 / 折叠 / 外链 / 徽标；数据层占位与路由守卫） | 737 **DISABLED**（旧件停用）；738 CONFIRMED（不变） |
| 28 | 739 ~ 741 | 3 | **阶段四 03-05 框架与容器**（主框架壳与宿主编排 / 表单框架壳 / 页面容器；仅 frontend；域 03 收官批次） | 739 CONFIRMED（不变）；740 / 741 **DISABLED**（旧件停用） |
| 29 | 742 / 743 | 2 | **阶段四 04-01 滚动容器**（滚动区 / 触底触顶 / 暴露方法；位置存储与保持；双端同号，两端各自执行） | CONFIRMED |
| 30 | 744 / 745 | 2 | **阶段四 04-02 懒加载与虚拟列表**（懒加载容器：视口渲染 / 占位 / 缓存与降级；虚拟列表容器：可视区 / 动态高度 / 触底 / 定位；双端同号） | CONFIRMED |
| 31 | 746 ~ 748 | 3 | **阶段四 04-03 视口与自适应**（全屏容器：请求 / 降级 / 状态同步 / 首次提示；自适应高度容器：父剩余空间 / 重算去抖 / 内部滚动；宽高比容器：比例解析 / 填充 / 占位与降级；双端同号） | CONFIRMED |
| 32 | 749 / 750 | 2 | **阶段四 04-04 状态与分区**（加载遮罩容器：四态互斥 / 延迟显示 / 重试；分区容器：标题 / 折叠 / 档位与令牌；双端同号；**域 04 收官批次**） | CONFIRMED |
| 33 | 751 ~ 755 | 5 | **阶段四 03-02-1 移动端反馈件（嵌套子任务）**（移动端错误页 / 空状态 / 骨架屏 / 内容遮罩 / 反馈状态机；`@bms/ui-vant` 与宿主，与 PC 同契约；2026-09-17 新增） | CONFIRMED |
| 34 | 760 ~ 762 | 3 | **阶段四 08-01 交互类占位版交付（3 嵌套）**（`08_01_01` 配置与工作流三件 + 占位契约；`08_01_02` 数据进出与表单五件 + 五能力投影；`08_01_03` 报表大屏与 AI 四件 + 两投影；均含分包懒加载入口；仅 frontend） | CONFIRMED |
| 35 | 763 | 1 | **阶段四 08-02 图标选择器与代码表达式编辑器**（图标注册表唯一性 + 选渲三件 + 四类编辑器与只读高亮 + 内核复用不重复加载与分包；仅 frontend） | CONFIRMED |
| 36 | 764 | 1 | **阶段四 08-03-01 向导与偏好设置（嵌套子任务）**（向导编排能力：可见步骤 / 分步与整体校验定位 / 已到达步跳转 / 分支回退 / 草稿 / 结果态；偏好设置：全量键即时预览 / 保存与回滚 / 恢复默认 / 租户受控 / 本地持久化与待同步；仅 frontend） | CONFIRMED |
| 37 | 765 | 1 | **阶段四 08-03-02 批量操作与主题租户（嵌套子任务）**（批量操作：选中集合 / 跨页全选 / 权限过滤 / 危险与超量确认 / 进度与部分成功 / 完成后清空；主题与品牌：解析优先级 / 系统跟随 / 禁用暗色 / 品牌色派生六项 / 根元素注入与防闪烁；租户切换：列表搜索 / 阶段推进 / 失败回退 / 单租户隐藏；仅 frontend） | CONFIRMED |
| 38 | 766 | 1 | **阶段四 08-03-03 打印与导出 PDF（嵌套子任务）**（打印模板：页眉 / 标题 / 字段 / 明细 / 汇总 / 签章 / 页脚 + 纸张方向与黑白 + 水印；核心预分页：行容量 / 页码连续 / 每页表头重复 / 空数据单页；预览：显隐 / 缩放夹取 / 多页 / 单页纸复用 / 空态；编排：浏览器打印 / 导出占位与阶段推进 / 失败重试 / 批量逐份合并 / 许可与权限过滤；仅 frontend） | CONFIRMED |
| 39 | 767 / 768 | 2 | **阶段四 08-04 权限配置（2 嵌套）**（`08_04_01` 授权编排基类与领域：权限树隐含推导与三态级联 / 字段权限默认全开与收窄 / 数据范围默认无 / 主体绑定去重与上限 / 载荷稳定序列化与内容派生幂等键 / 阶段机与上下文刷新；`08_04_02` 授权组件与宿主核对页：两投影 + 五件（授权总容器 / 权限树 / 字段权限矩阵与单元格 / 数据范围 / 主体绑定）+ 宿主机对页 12 项；仅 frontend） | CONFIRMED |
| 40 | 769 / 770 | 2 | **阶段四 08-05 导入导出（2 嵌套）**（`08_05_01` 导入导出编排基类与领域：文件校验与内容派生幂等键 / 导入阶段与错误行报告与分页截断 / 上传引擎进度与取消协作 / 导出决策与同步·异步两段轮询 / 下载取址三通路与降级重试 / 两套契约套件 + 既有基类向后兼容扩展；`08_05_02` 导入导出组件与宿主核对页：三投影 + 四件迁入 `import-export` 专题 + 新增错误报告件 + 件层下载触发工具 + 宿主机对页 12 项） | CONFIRMED（两子任务均已完成，2026-09-19） |
| 41 | 771 | 1 | **阶段四 08-06-1 表单设计器（嵌套子任务）**（布局元数据契约：装载归一 / 空布局回退默认栅格 / 三级层级解析与逐级回退 / 结构操作不可变幂等（插入·移动·跨列·分区增删改）/ 自建字段列名派生与类型白名单 / 失效与重复引用识别 / 校验与保存许可 / 脏基线比对 / 渲染输入与设计产出同形；设计器编排能力基类：占位零请求 / 取数基线 / 只读不动作 / 拖拽落点与广播 / 脏拦截 / 保存发布恢复默认 / 自建字段与 DDL 重试 / 防重复；两套契约套件 + 四件组件（设计器 / 画布 / 属性面板 / 自建字段对话框，三件分包）+ 拖拽适配工具 + 宿主核对页 12 项；仅 frontend） | CONFIRMED（`08-6-1` 已完成，2026-09-19；`08-6-2` 表单渲染器待实施） |
| 42 | 772 | 1 | **阶段四 08-06-2 表单渲染器（嵌套子任务）**（渲染领域：字段类型→控件语义键分发与未知回退 / 三态基线 / 权限双向叠加 / 渲染计划（分区·分组·未分组迁移·跨列·失效跳过·停用转只读·明细列回退）/ 规则构建与编译（复用既有校验器）/ 单字段与全量校验与首个错误定位 / 明细行级校验 / 脱敏与空值展示 / 提交载荷与许可；能力基类：占位零请求 / 取数两段与详情追加 / 只读不写 / 即时与全量校验 / 明细增删改 / 提交与重试 / 防重复；契约套件 `describeFormRendererContract` + 投影 + 分发映射工具 + 两件（迁入 `form-render` 专题目录，主体分包）+ 宿主核对页 12 项；仅 frontend） | CONFIRMED（`08-6-2` 已完成，2026-09-19） |
| 43 | 773 | 1 | **阶段四 08-7-1 文案目录编排基类与领域（嵌套子任务）**（文案目录领域：语言清单归一与四类校验（44001 / 44002 / 44004 / 44005）/ `msg_key` 命名轻校验 44003 与值长上限 / 缺失派生（前端派生 ∪ 后端标记、按启用语言、清单顺序）/ 筛选（模块前缀·关键词·缺失·仅已修改·语言聚焦）与筛选参数剔空下发 / 变更集与脏基线 / 内容派生幂等键 `i18n:{fnv1aHex}` / 分页夹取与虚拟滚动阈值判定 / 错误码文案与导出文件名；能力基类 `BaseMessageCatalog`：占位零请求 / 取数与基线 / 编辑与增删 key / 语言清单增改启停与保护规则 / 批量保存与缓存失效（失败降级不阻断）与语言包重载（版本递增）/ 脏基线与撤销；契约套件 `describeMessageCatalogContract`；仅 frontend） | CONFIRMED（`08-7-1` 已完成，2026-09-19） |
| 44 | 774 | 1 | **阶段四 08-7-2 文案目录组件与宿主核对页（嵌套子任务）**（投影 `useBaseMessageCatalog`（薄适配 + 下发面同步置基线）；专题族 `components/i18n/` 四件：编辑器容器（页签装配·工具栏·脏数据拦截·保存→失效→重载即时生效·导入导出接线）/ 语言清单（增改启停·默认语言与至少一种启用保护·弹窗表单）/ 筛选器（五类条件·本地草稿即时渲染·缺失提示）/ 文案网格（可编辑单元格·缺失高亮·停用列置灰仍占位·被修改标记·受控分页·超阈值虚拟滚动，独立分包懒加载）+ 宿主核对页 12 项；`08_01_02` 冻结对外契约与分包入口全部保持；仅 frontend） | CONFIRMED（`08-7-2` 已完成，2026-09-19） |
| 45 | —（原记 775） | — | **阶段四 08-8-1 审批流展示（嵌套子任务）**（审批流领域 `domain/approval`：归一（装载输入口径→运行态）/ 会签聚合与进度摘要 / 当前节点定位 / 分支降级（紧凑省略·非紧凑置 skipped）/ 时间线正倒序与节点分组 / 意见折叠与 **Unicode 码点**计数与校验（上限 4000）/ 动作前置校验与可用矩阵 / 内容派生幂等键 `wf:{fnv1aHex}` / 错误码处置映射（60003 / 60004 / 60005 / 60008 / 60009）/ 只读判定与只读图三级形态判定；能力基类 `BaseApprovalFlow`：占位零请求 / 实例与记录**并行取数**（分部结算·单失败不整体报错）/ 提交（幂等键·防重复·提交后刷新）/ 错误码处置（60005 按成功·60009 置只读·refresh 后复原失败面）/ 只读图图片通路（注入处理函数优先·否则预签名·皆无不请求）；契约套件 `describeApprovalFlowContract` + 投影 `useBaseApprovalFlow` + 专题族 `components/approval/` 五件（容器 / 进度 / 时间线 / 操作面板 / 只读图）+ `bpmn-js@18.28.0` Viewer 真实渲染（独立分包）+ 件层动作元数据与 bpmn-js 单一落点工具 + 宿主核对页 12 项；仅 frontend） | **已补登 974**（2026-09-21 补登；原记编号 775 实为 07-6 图表卡，见第 47 行） |
| 46 | —（原记 776） | — | **阶段四 08-8-2 流程建模器（嵌套子任务）**（建模领域 `domain/process-modeler`（**零依赖**纯字符串）：BPMN 子集结构提取（忽略命名空间前缀·容忍属性顺序与空白）/ 结构键与往返等价 / 结构校验五项（子集外元素·起止事件·id 唯一·引用完整·孤立节点）与元素定位 / 属性 schema 与读取（含 `bms:*` 扩展属性）与校验 / 校验结论合并（结构 + 引擎）/ 版本推进 / 内容派生幂等键 `wfd:{fnv1aHex}` / 错误码定位（60006 元素级·60007 定义级）；能力基类 `BaseProcessModeler`：占位零请求 / 定义装载（空 XML 回落模板·记脏基线）/ XML 更新与导入两段确认（校验通过才生效）/ 校验（结构 + 注入式引擎合并）/ 保存草稿与发布（幂等键·版本推进·**先占位再校验防并发穿透**）/ 脏基线与撤销 / 只读判定 / 错误码与重试；契约套件 `describeProcessModelerContract` + 投影 `useBaseProcessModeler` + 专题族四件（容器 / 调板 / 画布 / 属性面板）+ `bpmn-js@18.28.0` **Modeler** 可编辑画布（共用同一 `bpmn` 分包）+ 自建属性面板（条件表达式复用表达式编辑器）+ `bpmn-moddle` 强比对工具 + 宿主核对页 12 项；仅 frontend） | **已补登 975**（2026-09-21 补登；原记编号 776 实为 `08-9-1`，见第 48 行） |
| 47 | 775 | 1 | **阶段四 07-6 图表卡**（`BaseChart` 组件基类：实例生命周期 / 尺寸自适应节流 / 主题令牌重建 / 四态与空态 / 导出 / 注入式引擎适配器 / 占位零请求 + 领域纯函数 13 类型与映射与令牌主题与选项生成 + 契约套件 `describeChartContract` + 投影 + ECharts 内核独立分包 + 宿主核对页 12 项；2026-09-21 平台回读补记） | CONFIRMED |
| 48 | 776 / 777 | 2 | **阶段四 08-9-1 报表设计器 / 08-9-2 大屏设计器与播放**（`08-9-1`：`BaseReportDesigner` 编排基类 + `domain/report-layout`（12 列网格 / 校验 / 序列化 / 脏比对）+ 契约套件 + 专题族五件 + gridstack 独立分包 + 复用 07_06 图表渲染；`08-9-2`：`BaseScreenDesigner` / `BaseScreenPlayer` + `domain/screen-layout` 与 `screen-play` + 契约套件 + 专题族七件 + vue-flow 独立分包 + 多页轮播与自适应；均含宿主核对页 12 项；2026-09-21 平台回读补记） | CONFIRMED |
| 49 | 756 ~ 759 | 4 | **阶段四 05-01 ~ 05-04（旧双端体系）**（输入域口径包装与文本三件 / 数字框与下拉框 / 选择三件 / 双端收口与核对；所指实现随旧层删除） | **DISABLED**（2026-09-21 执行，保留编号不复用） |
| 50 | 778 ~ 781 | 4 | **后端补充基座批**（02-3-15 工厂基类 / 02-6-1 接口路由基座 / 02-4-23 用户偏好基座 / 02-4-20 列表查询与偏好契约基座；2026-09-21 平台回读补记） | CONFIRMED |
| 51 | 906 | 1 | **后端 02-4-25 打印模板与导出 PDF 基座**（两契约 / 占位实现 / 错误码子段；2026-09-21 平台回读补记） | CONFIRMED |
| 52 | 927 | 1 | **后端 02-4-26 自定义图标与代码校验基座**（两域契约 / 占位实现 / 表声明 / 依赖与限流；2026-09-21 平台回读补记） | CONFIRMED |
| 53 | 958 ~ 961 | 4 | **阶段四前端后续批次 1**（`07_05` 数据呈现 958 / `07_04` 通知与消息 959 / `08_10` AI 助手 960 / `07_07` 全局搜索 961；2026-09-21 平台回读补记） | CONFIRMED |
| 54 | 962 ~ 966 | 5 | **阶段四前端后续批次 2 与后端字典**（`06_05` 组织选择 962 / `02_4_27` 字典真实取数与缓存 963 / `06_06` 字典字段 964 / `06_07` 文件上传 965 / `06_04` 验证码 966；2026-09-21 平台回读补记） | CONFIRMED |
| 55 | 967 ~ 973 | 7 | **阶段四域 05 与域 06/07 首批补登（2026-09-21 登记）**（`05_01` 基础控件 967 / `06_01` 字段占位版 968 / `06_02` 基础与数据源字段 969 / `06_03` 富文本与高级交互 970 / `07_01` 展示占位版 971 / `07_02` 轻量展示件 972 / `07_03` 文件预览与审计差异 973；见 §3） | CONFIRMED（2026-09-21 补登） |
| 56 | 976 | 1 | **阶段五 01_01 扩展点注册表补齐与统一装配**（八类扩展点注册表（路由·菜单 / 页面区域 / 通用组件 / 字段渲染器 / 图标 / 工作台卡片 / 主题令牌 / i18n 文案包）公共契约：登记 / 解析 / 拒重 / 未命中 / 保序 / 只读快照；统一装配器：平台自身注册与模块注册同通道、来源打标、两段式校验与冲突回滚、逆序清理；键与标识校验（命名空间键 / 点分区域标识 / 小写语言标识 / 路由路径）；「不注册不可用」护栏 + fixture；2026-09-21 登记） | CONFIRMED |
| 57 | 977 | 1 | **阶段五 01_02 宿主加载器运行时化与模块契约远端对接**（模块清单契约与严格解析（三字段齐全 / 逐项拒绝 / 非数组抛错）；清单驱动加载器（入口懒加载表、名称与版本严格一致、上下文冻结、拒绝口径）；上下文契约收敛（单一类型 + 只读快照）；八类声明通道补齐字段渲染器与路由菜单图标；令牌机制（汇聚 / 应用 / 还原）与模块文案承载（并入 / 还原 / 未命中不兜底）；宿主清单获取与逐条挂载、失败归集、卸载逆序清理、菜单取注册表快照；区域插槽件；2026-09-21 登记） | CONFIRMED |
| 58 | 978 | 1 | **阶段五 02_01 Module Federation 接入与模块独立构建**（清单新增 `mode` 形态判据（缺省 `local`；`remote` 须绝对入口 URL，非法值与相对路径逐项拒绝）；加载器入口解析器注入（本地表解析器落 core、远端 MF 解析器落宿主，同一加载器同一校验链，`LocalModuleLoader` 删除）；远端入口解析器（`registerRemotes` 带 `type: module` 与 `force` 幂等 + `loadRemote('<模块名>/module')`，错误原样上抛）；宿主按 `mode` 分派与两形态共存；模块独立工程（`frontend/modules/demo`：MF remote / 独立安装 / 独立构建 / 独立预览壳）；模块产物单独计量（入口闭包阈值 + 入口块数上限 + 页面块不进入口）；2026-09-21 登记） | CONFIRMED |
| 59 | 979 | 1 | **阶段五 02_02 依赖共享与版本偏斜治理**（共享声明单一来源（共享面白名单 + 版本要求 + 受控非共享项）；宿主提供方 / 模块消费方语义（`import:false` 无本地回退副本 + `strictVersion` 版本不满足即拒绝）；**构建期**产物级断言（两侧声明一致 / 模块无副本 / 依赖实例数 = 1）与白名单 · 体积护栏；**运行期**实例数断言（真实 MF 运行时：共享域内每依赖版本条目数恒为 1、版本满足要求、major 不一致被拒）；受控实测结论（UI 组件库与基座包锁定为受控非共享项）；升级契约成文；2026-09-21 登记） | CONFIRMED |

## 2.1 用例迁移与下线（S5c，2026-09-17） <a id="migration"></a>

> 背景：基座框架无关核心重构（专项 `02-07`）将旧组件目录全量迁入 `@bms/ui-ep` 插件（S3，34 件），
> 宿主经 S5a 切流、S5b 迁 `apps/desktop`；S5c 删除 `apps/desktop` 旧件与旧 spec。

| 范围（apps 侧 = 原 frontend） | 处置 | 承接 |
| --- | --- | --- |
| 724 / 725（弹窗抽屉表单）、726 ~ 730（异常与空状态）、731 ~ 736（结构布局）、737（侧边菜单渲染）、740 / 741（表单框架壳 / 页面容器）、742 ~ 750 的 apps 侧执行（容器域 04-01 ~ 04-04） | **DISABLED**（apps 侧用例随旧件删除下线，2026-09-17） | `frontend/packages/ui-ep` 用例套件（59 条，`frontend/packages/ui-ep/tests/*.spec.ts`）+ 宿主用例 739（保留，切 ui-ep 断言） |
| 698 / 699（根系）、700 ~ 704（组件根基类）、705 ~ 708（能力机制）、709 ~ 712（域基类）、713 ~ 716（护栏）、717 / 718（请求）、719 / 720（权限）、721 / 722（格式化）、738（菜单数据层与守卫） | **CONFIRMED 不变**（能力层 / 基础设施用例随迁 `frontend/apps/desktop/tests/`，断言集不变） | — |
| `frontend/apps/mobile` 侧（742 ~ 750 等双端同号中 mobile 侧执行） | **CONFIRMED 不变**（S4c 2026-09-17 迁入 `frontend/packages/ui-vant/tests/`，Kiwi 原号保持） | `frontend/packages/ui-vant/tests/container-*.spec.ts`（9 spec；承接套件含契约用例共 64 条） |
| `frontend/apps/mobile` 侧旧层用例（`base-*` / `guard-*` / `format-registry` / `container-*` 等 28 spec） | **随层删除（不影响平台状态）**——对应编号 698 ~ 722 / 738 / 739 由 apps 侧继续执行（CONFIRMED 不变），724 ~ 737 / 740 / 741 已在下方统一停用 | 核心断言转 `@bms/core` / `@bms/vue` / `ui-vant` 套件承接；宿主用例（719 / 720 / 721 / 722 / 723 等）保留并改造（判定经插件注入点） |

- 迁移口径：能力断言不减少——旧件用例的覆盖点已由 `ui-ep` 套件（S3 迁移时逐件移植，累计 59 条）承接；
  双端同号语义继续（mobile 侧迁移后进行两端对账）。
- 平台侧操作（**2026-09-17 已执行**）：apps 侧旧件用例 **724 ~ 737 / 740 / 741（16 条）置 `DISABLED`**——
  经 `test/scripts/kiwi/update_status.py --ids … --status DISABLED`（服务器 Kiwi 容器 Django shell）执行并回读核对：
  16 条 `DISABLED`、698 ~ 722 / 738 / 739 / 742 ~ 750 保持 `CONFIRMED`；快照取证
  `test/scripts/kiwi/exports/2026-09-17_策展用例快照.json`（145 条）。
- 平台侧操作（**2026-09-17 已执行，范围更正**）：mobile 侧旧层 spec 的删除**不改变平台状态**（同号用例由 apps 侧
  继续执行或已按上条停用）；原「mobile 侧旧层置 DISABLED」表述作废。

## 3. 最新批次明细：阶段五 01_01 / 01_02 / 02_01 / 02_02（2026-09-21） <a id="latest"></a>

### 3.1 阶段五 01_01 扩展点注册表补齐与统一装配（976） <a id="batch-01-01"></a>

| 项 | 值 |
| --- | --- |
| 登记日期 | 2026-09-21 |
| 来源 | `bms/bms文档/项目/05_前端插件化/任务/01_扩展点与装配/01_扩展点与装配_01_扩展点注册表补齐与统一装配/`（需求 05-1） |
| 平台编号（回读） | **976** |
| 自动化文件 | `bms/frontend/packages/core/tests/`：`registries.spec.ts`、`assemble.spec.ts`、`guard-unregistered-provider.spec.ts`；`bms/frontend/apps/desktop/tests/`：`module-registration.spec.ts`——每文件首行 `// kiwi_id: 976` |
| 覆盖点 | 八类扩展点注册表公共契约（登记 / 按键解析 / 同键拒重 / 未命中返回空 / 遍历保序 / 只读快照 / 幂等注销）；三类新增域专有解析（区域 `resolveByArea` 保序与区域去重、主题令牌 `byMode`、文案包 `byLocale` 大小写归一）；键与标识校验（命名空间键 / 点分区域标识 / 小写语言标识 / 路由路径前缀）；统一装配器（平台与模块同通道、来源打标、前缀越权拒绝、八类分组登记键、两段式零登记与冲突回滚、逆序清理）；「不注册不可用」护栏与 fixture |
| 状态 | 976 CONFIRMED（分类「平台骨架」、优先级 P2、标签「自动化」） |
| 执行结果 | 按「一任务一条」登记；对应自动化文件随 `npm run check` 的 core 套件（89 文件 / 1134 用例）与宿主套件（11 文件 / 38 用例）执行，全绿 |
| 备注 | 阶段五首个任务用例；前批（阶段四域 05 与域 06/07 首批补登 967 ~ 973）明细见 git 历史；其后批（`01_02`）取 977，见 §3.2 |

### 3.2 阶段五 01_02 宿主加载器运行时化与模块契约远端对接（977） <a id="batch-01-02"></a>

| 项 | 值 |
| --- | --- |
| 登记日期 | 2026-09-21 |
| 来源 | `bms/bms文档/项目/05_前端插件化/任务/01_扩展点与装配/01_扩展点与装配_02_宿主加载器运行时化与模块契约远端对接/`（需求 05-2） |
| 平台编号（回读） | **977** |
| 自动化文件 | `bms/frontend/packages/core/tests/`：`module-manifest.spec.ts`、`module-loader.spec.ts`、`theme-tokens.spec.ts`、`i18n-packs.spec.ts`、`assemble.spec.ts`（字段渲染器与路由图标分组）；`bms/frontend/packages/ui-ep/tests/`：`module-area-outlet.spec.ts`；`bms/frontend/apps/desktop/tests/`：`module-manifest.spec.ts`、`module-hosting.spec.ts`、`module-registration.spec.ts`——前四份与 ui-ep、宿主两份**首行**标 `// kiwi_id: 977`；`assemble.spec.ts` 在新增分组处标注；`module-registration.spec.ts` 首行承接 `976`（挂载通路改清单驱动），行内注明本任务 `977` |
| 覆盖点 | 模块清单严格解析（三字段齐全 / 缺字段与名非法逐项拒绝 / 重名保留先通过项 / 非数组抛错）；清单驱动加载器（清单名与入口补入 manifest、上下文冻结、未登记名 / 入口未登记 / 缺默认导出 / 名称与版本不一致拒绝、挂载卸载幂等、与本地实现同接口）；上下文契约收敛；字段渲染器声明通道与路由菜单图标；令牌汇聚 / 应用 / 还原；模块文案承载并入 / 还原 / 未命中不兜底；宿主清单获取失败不阻塞启动、逐条挂载与失败归集、消费接线成对（令牌写入与移除 / 文案并入与还原 / 区域项出现与消失）、装配冲突回滚、菜单取注册表快照；区域插槽件空区域 / 保序 / 异步组件解析 / 装配版本号重算 |
| 状态 | 977 CONFIRMED（分类「平台骨架」、优先级 P2、标签「自动化」） |
| 执行结果 | 按「一任务一条」登记；对应自动化文件随根 `npm run check`（core 93 文件 / 1155 用例、ui-ep 61 / 901）与宿主 `test:cov`（12 文件 / 47 用例）执行，全绿；宿主覆盖率 87.71% |
| 备注 | 阶段五域 01 收口用例；前批（`01_01`）为 976，见 §3.1；其后批（`02_01`）取 978，见 §3.3 |

### 3.3 阶段五 02_01 Module Federation 接入与模块独立构建（978） <a id="batch-02-01"></a>

| 项 | 值 |
| --- | --- |
| 登记日期 | 2026-09-21 |
| 来源 | `bms/bms文档/项目/05_前端插件化/任务/02_运行时加载/02_运行时加载_01_ModuleFederation接入与模块独立构建/`（需求 05-3） |
| 平台编号（回读） | **978** |
| 自动化文件 | `bms/frontend/packages/core/tests/`：`module-manifest.spec.ts`、`module-loader.spec.ts`、`module.spec.ts`（均含本任务新增断言；前两份首行承接 `977`，新增处注明）；`bms/frontend/apps/desktop/tests/`：`module-federation.spec.ts`（**首行** `// kiwi_id: 978`）、`module-hosting.spec.ts`、`module-registration.spec.ts`、`module-manifest.spec.ts`（远端形态与分派断言加注 `978`）；`bms/frontend/modules/demo/tests/module-definition.spec.ts`（**首行** `// kiwi_id: 978`） |
| 覆盖点 | 清单 `mode` 形态判据（缺省归一 / 非法值拒绝 / `remote` 非绝对 URL 拒绝 / 重名口径不变）；入口解析器注入（本地与远端同接口同校验链、本地未登记抛 `PROVIDER_NOT_REGISTERED`、解析器抛错原样上抛）；远端入口解析器（按清单登记容器 `{name, entry, type: module}` + `force` 幂等、按 `<模块名>/module` 加载、不可达错误上抛、空返回交加载器判定）；宿主按 `mode` 分派与两形态共存；远端挂载后动态路由与区域 / 令牌 / 文案消费成对生效与还原；旧加载器移除（`LocalModuleLoader` 用例改写为解析器用例）；模块定义契约（默认导出、清单一致、路由懒加载、八类声明键前缀与区域标识 / 语言标识 / 令牌变量）；模块产物计量（入口闭包阈值、入口块数上限、页面块不进入口、入口外异步块数下限） |
| 状态 | 978 CONFIRMED（分类「平台骨架」、优先级 P2、标签「自动化」） |
| 执行结果 | 按「一任务一条」登记；对应自动化文件随根 `npm run check`（core 93 文件 / 1158 用例、vue 2 / 5、ui-ep 61 文件）与宿主 `test:cov`（13 文件 / 53 用例，语句 88.88%）执行；模块工程 `test`（1 文件 / 4 用例）与 `build + budget`（入口闭包 8.8 KB / 6 块）全绿；端到端以模块构建产物（`vite preview` 5002）+ 宿主构建产物（`vite preview` 4173）实跑验证 |
| 备注 | 阶段五域 02 首条用例；前批（`01_02`）为 977，见 §3.2；其后批（`02_02`）取 979，见 §3.4 |

### 3.4 阶段五 02_02 依赖共享与版本偏斜治理（979） <a id="batch-02-02"></a>

| 项 | 值 |
| --- | --- |
| 编号 | **979** |
| 任务 | 阶段五 `02_02` 依赖共享与版本偏斜治理（需求 `05-4`） |
| 分类 / 优先级 / 状态 / 标签 | 平台骨架 / P2 / CONFIRMED / 自动化 |
| 覆盖范围 | ① 共享声明单一来源与角色差异（宿主提供方 / 模块消费方 `import:false` + `strictVersion`）；② 构建期产物级断言（两侧声明一致、模块无副本、依赖实例数 = 1）；③ 白名单与实装版本、非共享项体积护栏；④ 运行期共享域实例数断言（版本条目数恒为 1）；⑤ 受控实测结论（UI 组件库 / 基座包锁定为受控非共享项）与升级契约成文 |
| 自动化文件 | `frontend/apps/desktop/tests/shared-dependencies.spec.ts`（Kiwi 用例标注于文件头注释）；构建期断言 `frontend/scripts/check-shared-deps.mjs` / `check-shared-whitelist.mjs`（CI job `shared-deps-check`） |
| 备注 | 阶段五域 02 收口用例；前批（`02_01`）为 978，见 §3.3；**下次登记从 980 起** |

## 4. 对账结果 <a id="reconcile"></a>

对账时间：2026-09-16（`export_cases.py` 实时导出 + 手工核对前端标记）；2026-09-19 补记阶段四域 08 批次（760 ~ 772，平台回读）；2026-09-21 补记阶段五批次（976 / 977 / 978 / 979，登记回读）。

| 侧 | 结果 |
| --- | --- |
| 后端（`--reconcile --code-root ../bms/backend`） | 平台 103 条 / 代码引用 80 个 / **matched 78** |
| 后端 `only_code`（代码引用了但本产品查不到） | 533、534 —— **口径假阳性**：两条实际存在于 `BMS 自动化用例（CI）` 产品（`tests/core/test_plugin.py` 的两个函数），`--reconcile` 只查策展产品所致 |
| 后端 `only_platform` | 25 条（`BMS 自动化用例（CI）` 导入记录，属预期） |
| 前端（手工核对，脚本未覆盖） | `frontend/apps/desktop` / `frontend/apps/mobile`：19 / 21 / 23 / 25 / 698、20 / 22 / 24 / 26 / 699；02-02 的 **700 ~ 704**、02-03 的 **705 ~ 708**、02-04 的 **709 ~ 712**、02-05 的 **713 ~ 716**、02-06-01 的 **717 / 718**、02-06-02 的 **719 / 720**、02-06-03 的 **721 / 722**、03-06 的 **723**（双端同号）与 03-01 的 **724 / 725**、03-02 的 **726 ~ 730**、03-03 的 **731 ~ 736**、03-04 的 **737 / 738**（仅 frontend）、03-05 的 **739 ~ 741**（仅 frontend）、04-01 的 **742 / 743**、04-02 的 **744 / 745**、04-03 的 **746 ~ 748** 与 04-04 的 **749 / 750**（双端同号）—— **全部命中平台**，无孤儿引用；**阶段四域 02（698 ~ 722）/ 域 03（723 ~ 741）/ 域 04（742 ~ 750，收官）用例全部登记完成** |
| 前端（2026-09-19 补记，域 08 批次） | `08_01` 的 **760 ~ 762**（占位版交付三嵌套）、`08_02` 的 **763**（图标与代码编辑器）、`08_03_01` 的 **764**（向导与偏好设置）、`08_03_02` 的 **765**（批量操作与主题租户）、`08_03_03` 的 **766**（打印与导出 PDF）—— 平台回读一致、代码标注一致，无孤儿引用 |
| 前端（2026-09-19 再补记，域 08 权限配置与导入导出） | `08_04_01` 的 **767** / `08_04_02` 的 **768**（授权编排基类与领域 / 授权组件与宿主核对页）—— 平台回读一致、代码标注一致（实施记录与测试记录均已含 767 / 768）；`08_05_01` 的 **769**（导入导出编排基类与领域）与 `08_05_02` 的 **770**（导入导出组件与宿主核对页）同上——两子任务均已完成（2026-09-19）、代码首行标注 `// kiwi_id: 769` / `// kiwi_id: 770`、无孤儿引用 |
| 前端（2026-09-19 三补记，域 08 表单设计器） | `08-6-1` 的 **771**（表单设计器：布局元数据契约 + 设计器编排能力基类 + 两套契约套件 + 四件组件与分包 + 拖拽适配工具 + 宿主核对页 12 项）—— 平台回读一致、代码标注一致（三个用例文件首行均标 `// kiwi_id: 771`；实施与测试记录均已含 771）、无孤儿引用；`08-6-2` 表单渲染器待实施（顺延登记 772）；**域 06 / 07 的 6 个任务尚无平台编号**（§6 待办 5） |
| 前端（2026-09-19 四补记，域 08 表单渲染器） | `08-6-2` 的 **772**（表单渲染器：渲染领域 `domain/form-render` + 渲染编排能力基类 `BaseFormRenderer` + 契约套件 + 投影 + 分发映射工具 + 两件组件与分包 + 宿主核对页 12 项）—— 平台回读一致、代码标注一致（三个用例文件首行均标 `// kiwi_id: 772`；实施与测试记录均已含 772）、无孤儿引用；**阶段四域 08 `08_01` ~ `08_06` 用例全部登记完成**；**域 06 / 07 的 6 个任务尚无平台编号**（§6 待办 5） |
| 前端（2026-09-19 五补记，域 08 国际化文案编辑器） | `08-7-1` 的 **773**（文案目录编排基类与领域：领域 `domain/i18n` + 能力基类 `BaseMessageCatalog` + 契约套件）+ `08-7-2` 的 **774**（投影 + 专题族 `components/i18n/` 四件 + 宿主核对页 12 项）—— 平台回读一致、代码标注一致（三个用例文件首行分别标 `// kiwi_id: 773` / `// kiwi_id: 774`；实施与测试记录均已含 773 / 774）、无孤儿引用；**阶段四域 08 `08_01` ~ `08_07` 用例全部登记完成**；**域 06 / 07 的 6 个任务尚无平台编号**（§6 待办 5） |
| 前端（2026-09-19 六补记，域 08 审批流展示与流程建模器） | `08-8-1` 的 **775**（审批流展示：领域 `domain/approval` + 能力基类 `BaseApprovalFlow` + 契约套件 + 投影 + 专题族五件 + `bpmn-js` Viewer 只读图 + 宿主核对页 12 项）+ `08-8-2` 的 **776**（流程建模器：领域 `domain/process-modeler`（零依赖）+ 能力基类 `BaseProcessModeler` + 契约套件 + 投影 + 专题族四件 + `bpmn-js` Modeler 画布 + `bpmn-moddle` 强比对 + 宿主核对页 12 项）—— 平台回读一致、代码标注一致（两个用例文件首行分别标 `// kiwi_id: 775` / `// kiwi_id: 776`；实施与测试记录均已含 775 / 776）、无孤儿引用；**阶段四域 08 `08_01` ~ `08_08` 主线全部登记完成**；**域 06 / 07 的 6 个任务尚无平台编号**（§6 待办 5，2026-09-21 已闭环） |
| 前端（2026-09-21 补记，域 05 与域 06/07 首批补登） | `05_01` → **967**、`06_01` → **968**、`06_02` → **969**、`06_03` → **970**、`07_01` → **971**、`07_02` → **972**、`07_03` → **973** —— 平台回读一致、代码首行 `// kiwi_id` 标注一致、7 份任务测试记录已回填，无孤儿引用；同批更正 `775`（07-6 图表卡）与 `776` / `777`（`08-9-1` / `08-9-2`）的编号归属；补登 `08-8-1` / `08-8-2` 为 **974 / 975**、旧双端 `756` ~ `759` 置 `DISABLED` |
| 前端（2026-09-21，阶段五 `01_01`） | **976** —— 四个自动化文件首行均标 `// kiwi_id: 976`，任务测试记录已回填（含 Kiwi 编号），无孤儿引用；登记输入见 `scripts/kiwi/cases/2026-09-21_阶段五01-01_扩展点注册表补齐与统一装配.json`（含回读 `case_id`） |
| 前端（2026-09-21，阶段五 `01_02`） | **977** —— 九个自动化文件按「首行 / 新增分组处」标注 `// kiwi_id: 977`（`assemble.spec.ts` 与承接 976 的 `module-registration.spec.ts` 为分组 / 行内标注），任务实施与测试记录已回填（含 Kiwi 编号），无孤儿引用；登记输入见 `scripts/kiwi/cases/2026-09-21_阶段五01-02_宿主加载器运行时化与模块契约远端对接.json`（含回读 `case_id`） |
| 前端（2026-09-21，阶段五 `02_01`） | **978** —— 新文件首行标注 `// kiwi_id: 978`（宿主 `module-federation.spec.ts`、模块工程 `module-definition.spec.ts`），既有文件（core `module-manifest` / `module-loader` / `module.spec.ts`、宿主 `module-hosting` / `module-registration` / `module-manifest.spec.ts`）在新增断言处加注 `978`（文件首行承接原任务编号），任务实施与测试记录已回填（含 Kiwi 编号），无孤儿引用；登记输入见 `scripts/kiwi/cases/2026-09-21_阶段五02-01_ModuleFederation接入与模块独立构建.json`（含回读 `case_id`） |
| 前端（2026-09-21，阶段五 `02_02`） | **979** —— 新文件头注释标注 Kiwi 用例 `979`（宿主 `tests/shared-dependencies.spec.ts`）；构建期断言脚本（`frontend/scripts/check-shared-deps.mjs` / `check-shared-whitelist.mjs`）经 CI job `shared-deps-check` 执行，归属同一用例；任务实施与测试记录已回填（含 Kiwi 编号），无孤儿引用 |

## 5. 新增批次维护流程 <a id="flow"></a>

1. **先登记、后编码**（《测试规范》「用例管理（Kiwi TCMS）」节）：按 `examples/cases.example.json` 写输入文件，落在 `scripts/kiwi/cases/<日期>_<任务>_<名称>.json`。
2. `--dry-run` 自查容器内将执行的代码，再正式登记并**回读实际编号**（跳号属正常）。
3. 代码侧以平台编号标注：后端 `@pytest.mark.kiwi_id(N)`；前端 Vitest 用 `describe('…（Kiwi N）')`（前端仓库约定）。
4. 回填两处文档：bms 任务 `测试/NN_测试_xx.md`（用例表 + 结论）与本台账（§2 追加批次、§3 更新为最新批次）。
5. 需要留痕时用 `export_cases.py --out exports/<日期>_策展用例快照.json` 存平台快照（口径见 `scripts/kiwi/exports/README.md`）。

## 6. 待办 <a id="todo"></a>

| # | 待办 | 说明 |
| --- | --- | --- |
| 1 | `export_cases.py` 的对账范围延伸至前端 | 现只扫 `**/*.py` 的 `kiwi_id(...)`；前端 10 个引用（19 ~ 26、698 / 699）只能手工核对，建议支持 `describe('…（Kiwi N）')` 形态 |
| 2 | `--reconcile` 的 `only_code` 跨产品判定 | 只查策展产品会把 CI 自动用例产品的编号误报为「平台缺」（实证 533 / 534），建议加 `--include-ci` 或按产品分别比较 |
| 3 | 历史用例（1 ~ 61）补编号前缀 | 早期登记未带任务号前缀，任务归属需人工比对；新批次一律带任务号前缀，不再新增无前缀用例 |
| 4 | 批次的登记日期只对新增批次留痕 | 1 ~ 61 的登记日期未逐批记录（早期按主题登记），如需完整时间线可从平台 `date_created` 补齐 |
| 5 | ~~阶段四域 06 / 07 用例未登记平台~~ **已闭环（2026-09-21）** | 实际范围为域 05 `05_01` 与域 06 / 07 首批 `06_01` ~ `06_03`、`07_01` ~ `07_03`（**共 7 个任务**，台账原漏记 `05_01`）；已补登 **967 ~ 973** 并回填代码 `// kiwi_id` 标注与 7 份任务测试记录（见 §3）。 |
| 6 | `08_08` 无平台用例、代码 `kiwi_id` 错标 | 平台回读 `775` = 07-6 图表卡、`776` / `777` = `08-9-1` / `08-9-2`；`interaction-approval-flow.spec.ts` 与 `interaction-process-modeler.spec.ts` 首行标注的 `775` / `776` 属错标；`08-8-1` / `08-8-2` 在平台无独立用例。**已闭环（2026-09-21）**：补登 **974 / 975**，并修正两个 ui-ep spec 与 2 份任务记录的 `kiwi_id` 错标。 |
| 7 | 旧双端体系 `05-01` ~ `05-04`（`756` ~ `759`）仍 `CONFIRMED`、所指实现随旧层删除 | **已闭环（2026-09-21）**：经 `update_status.py --ids 756,757,758,759 --status DISABLED` 执行并回读为 `DISABLED`，保留编号不复用。 |

> 依《文档生成规范》编写 · 测试资产仓库
