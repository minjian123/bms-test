# 探索性测试记录 ET-2026-09-16-01

> 阶段四 02-01 根系基类：设计节点契约 ↔ 双端实现 ↔ 清单登记 三方一致性走查

模板见《[探索性测试记录模板](探索性测试记录模板.md)》。

## 1. 会话章程 <a id="charter"></a>

```text
会话编号：ET-2026-09-16-01
目标（charter）：阶段四首任务「根系基类」的三方一致性——组件设计节点声明的能力面、
              双端实现的实际导出面、《前端基类清单》的登记状态是否互相对得上；
              顺带核对 Kiwi 用例标记与平台的对应关系（用例侧台账见《Kiwi用例台账》）。
范围：bms 组件设计节点 02 基础组件类 · 01 机制基类 · 01 根系基类；
      bms frontend/apps/desktop/src/base、frontend/apps/mobile/src/base；
      bms frontend/apps/desktop/tests、frontend/apps/mobile/tests；
      《前端基类清单》§2 根系行与 §7 机制层文件口径。
不测什么：日志后端上报（未接入，随阶段八 / 十三）、i18n 词条内容质量、
          运行期性能（「无 DOM、无响应式」仅作静态确认，不做压测）。
时间盒：60 分钟（不延长，超出另开会话）
环境：分支 main；git 工作区干净（bms 侧 02_01 已提交 fefa6e5 / 517bf5b / 3d7eb0a）；
      Kiwi 平台：mjbk 容器 bms-kiwi（宿主 8060）
执行人：minjian（与 AI 结对，命令与观察见 §3）
```

## 2. 会话记录 <a id="log"></a>

> 记录要点：先记事实（命令 + 输出），再记判断。

| 时刻 | 操作 / 场景 | 观察 | 疑问 / 下一步 |
| --- | --- | --- | --- |
| — | `ls src/base`（双端） | 各 3 个文件：`BaseFrontend.ts` / `useFrontendBase.ts` / `BaseFrontend.vue` | 与设计节点 §2「组件清单与职责」3 行逐字一致 → 下一步比对能力面 |
| — | `diff -r frontend/apps/desktop/src/base frontend/apps/mobile/src/base` | 退出码 0，**无差异**；`tests/` 侧仅 Kiwi 编号行不同（19/20、21/22、23/24、25/26、698/699） | 与 §4「同接口多实现」约定一致 |
| — | `grep -nE '^export ' src/base/*.ts` | 导出面除 `BaseFrontend` / `useFrontendBase` 外，还有 `FrontendEnv` / `LogLevel` / `FrontendBaseOptions` / `LogRecord` / `ErrorRecord` / `LogSink` / `ErrorSink` / `FrontendRuntimeConfig` / `FrontendConfigPatch` 与 `resolveBuildConfig` / `configureFrontendBase` / `resetFrontendBaseConfig` / `getFrontendRuntimeConfig` / `setFrontendSinks` | 配置与 sink 的入口函数未在设计节点 §3 列出 → 记为发现 1 |
| — | 读 `UseFrontendBaseReturn` 与 `BaseFrontend` 成员 | 组合式返回 `ns` / `identifier` / `env` / `config` / `log` / `reportError` / `getConfig` / `t` / `dispose`；类侧另有 `protected onBaseCreated()` / `onBaseDisposed()`（构造时触发 created、`dispose` 幂等触发 disposed） | 与节点 §3「通用字段 / 通用方法」逐项对得上；`dispose` 未在 §3 表内 → 记为发现 2 |
| — | `grep -nE "^\s*import |from '" src/base/*` | `BaseFrontend.ts` → `@/i18n`；`useFrontendBase.ts` → `vue` + 同目录；`BaseFrontend.vue` → `vue` + 同目录 | 无 UI / store / 业务引用 ✓；但 `@/i18n` 属应用层模块 → 记为发现 3 |
| — | 读《前端基类清单》§2 与 §7 | 根系行：三文件列全、状态「已交付」；§7 机制层「除根系外各文件口径以对应《组件设计》节点 §2『位置』列为准」 | 登记状态与磁盘一致 ✓；组件根文件 `src/base/component.ts` 标注「规划」，与阶段四后续任务（02_02）匹配 |
| — | `grep` 双端 spec 的 Kiwi 标记 | `frontend/apps/desktop`：19 / 21 / 23 / 25 / 698；`frontend/apps/mobile`：20 / 22 / 24 / 26 / 699 | 与平台快照逐号比对，**全部命中** |
| — | `export_cases.py --reconcile --code-root ../bms/backend` | 平台 94 / 代码引用 80 / matched 78；`only_code` = [533, 534]；`only_platform` = 16 | 查 533 / 534 实际产品 → 见发现 4 |
| — | 平台查询 533 / 534 归属 | 两条存在，但产品是 `BMS 自动化用例（CI）`（`tests/core/test_plugin.py` 的两个函数） | `--reconcile` 只查策展产品 → 假阳性口径缺口 |
| — | 比对节点 §6.2 三项上游登记 | ① 前端基础类分层（已入《前端开发规范》§3）、② 组件分层与目录（已入《架构设计 · 前端架构》）均已闭环；③《概要设计 · 系统参数》「前端配置」节**仍缺** | 与 02_01 实施记录 §6 遗留一致，非本会话新增 |

## 3. 发现与缺陷 <a id="findings"></a>

| # | 现象 | 严重度 | 平台 | 处理 |
| --- | --- | --- | --- | --- |
| 1 | 设计节点 §3 只列 `log` / `reportError` / `getConfig` / `t` 与两个钩子，未列实现中对外可见的 `dispose()` 与配置 / sink 入口（`configureFrontendBase` / `resetFrontendBaseConfig` / `getFrontendRuntimeConfig` / `setFrontendSinks` / `resolveBuildConfig`） | P3 | 待定（文档口径） | 建议在该节点 §3 补「释放与配置入口」小节；**改 bms 设计文档需用户拍板**，本记录只登记建议 |
| 2 | 节点 §4「根系不得引用 UI、store、业务」的措辞下，实现 `import { i18n } from '@/i18n'` 的解释空间不明（i18n 既非 UI / store / 业务，也不是「上层」） | P3 | 待定（文档口径） | 建议在《前端开发规范》§3 或节点 §4 明确「i18n 委托属允许的横切依赖」；同样需用户拍板 |
| 3 | 登记项③《概要设计 · 系统参数》「前端配置」节缺失，`getConfig` / `env` 的上游配置口径无权威来源 | P2（上游文档缺口） | — | 已登记在 02_01 实施记录 §6 与阶段四计划 §3.1 第 6 行（归口阶段六 / 八），本次仅复核 |
| 4 | `export_cases.py --reconcile` 报 `only_code` = [533, 534]，实为 CI 自动用例产品中的编号；且脚本只扫 `**/*.py`，前端 10 个引用无法机核对 | P2（工具口径） | — | 已登记《Kiwi用例台账》§6 待办 1 / 2（待排期改脚本） |

> 三条文档 / 工具类问题均**未直接改动** bms 文档与脚本：按工作区规则，改设计文档与工具行为需先确认。

## 4. 会话收尾 <a id="wrap"></a>

```text
实际用时：未计时（本记录在比对完成后整理；全部操作与命令见 §2，可原样重放）
覆盖区域：设计节点 §2 / §3 / §4 / §6.2（已打）；§5 场景集成、§7 权限/i18n/移动端、§8 性能边界
          （仅读口径，未逐项验证）；双端 src/base 全量；双端 tests 的 Kiwi 标记；
          《前端基类清单》§2 根系行 + §7 机制层。
          未打：日志后端上报链路（未接入）、i18n 词条质量、原型 html 的交互逐帧走查。
发现的缺陷数（按级别）：P0 0 / P1 0 / P2 2 / P3 2
值得转成 Kiwi 用例的场景：无 —— 四项发现均属文档 / 工具口径，不是产品行为，不进 Kiwi 用例库
遗留疑问 / 下次 charter 建议：
  - 是否把 `dispose()` 与配置入口补进组件设计节点 §3（待用户拍板）；
  - i18n 依赖口径如何表述（规范 §3 或节点 §4）；
  - 下个会话建议覆盖：02_02 组件根基类（机制基类与组件根，重点核对「占位 / 异步资源 / 错误 / 订阅」四机制基类的文件口径）。
结论：☑ 可进入下一区域（02_02 组件根基类）　□ 该区域风险仍高，建议补充会话
```

> 依《文档生成规范》编写 · 测试资产仓库
